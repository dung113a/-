@model chuyende.Models.UserProfile

@{
    Layout = null;
}

<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Home - Cado</title>
    <link href="css/styles.css" rel="stylesheet"> <!-- Liên kết CSS -->
    <link href="/css/styles.css" rel="stylesheet">

    <link href="https://fonts.googleapis.com/css2?family=Open+Sans:wght@300;400;600&display=swap" rel="stylesheet">
</head>
<body>
    <div class="container">
        <!-- Profile Card -->
        @if (Model != null && !string.IsNullOrEmpty(Model.Una))
        {
            <div class="profile-card card">
                <img src="@Model.Uavatar" alt="Profile Picture" class="profile-image">
                <h3>@Model.Una</h3>
                <p>Email: @Model.Uemail</p>
                <p>Điểm: @Model.Star</p>
                <p>Ngày đăng ký: @Model.RegisterDate?.ToString("dd/MM/yyyy")</p>
            </div>
        }
        else
        {
            <div class="profile-card card">
                <img src="..." alt="Profile Picture" class="profile-image">
                <h3>Guest</h3>
                <p><a href="/Account/Login">Đăng nhập</a></p>
            </div>
        }

        <!-- Word Library -->
        <div class="card" data-detail="Manage and add new words to your library.">
            <i>📚</i>
            <h3>單字庫</h3>
            <p>Manage and add new words.</p>
        </div>

        <!-- My Groups -->
        <div class="card">
            <a href="/Groups" style="text-decoration: none; color: inherit;">
                <i>👥</i>
                <h3>我的群組</h3>
                <p>Manage study groups.</p>
            </a>
        </div>

        <!-- Words to Practice -->
        <div class="card" data-detail="List of words you need to practice.">
            <i>✏️</i>
            <h3>Words to Practice</h3>
            <p>Review your words.</p>
        </div>

        <!-- My Notes -->
        <div class="card" data-detail="Keep track of important notes.">
            <i>📝</i>
            <h3>我的筆記</h3>
            <p>Track your notes.</p>
        </div>

        <!-- Achievements -->
        <div class="card achievements" data-detail="View your achievements and progress.">
            <i>🏆</i>
            <h3>成就</h3>
            <p>View your progress.</p>
        </div>

        <!-- Biểu đồ -->
        <div class="card chart-card" data-detail="Nội dung của Biểu đồ">
            <i>📊</i>
            <h3>Biểu đồ</h3>
            <canvas id="lineChart" width="400" height="200"></canvas>
        </div>

        <!-- Activity -->
        <div class="card" data-detail="Check where you rank in the world.">
            <i>🆚</i>
            <h3>PK小遊戲</h3>
        </div>

        <!-- Account Settings -->
        <div class="card" data-detail="Manage your account settings.">
            <i>⚙️</i>
            <h3>使用者設定</h3>
        </div>

        <!-- Logout -->
        @if (Model != null && !string.IsNullOrEmpty(Model.Una))
        {
            <div class="card logout-btn" data-detail="You will be logged out.">
                <i>🚪</i>
                <h3 onclick="window.location.href='/Account/Logout'">登出</h3>
            </div>
        }
    </div>

    <!-- Details Panel -->
    <div class="details-panel">
        <h2 id="detail-title">Details</h2>
        <p id="detail-content">Select a card to view details.</p>
    </div>

    <script src="https://cdn.jsdelivr.net/npm/chart.js"></script> <!-- Chart.js -->
    <script src="js/scripts.js"></script> <!-- Liên kết JavaScript -->
</body>
</html>
